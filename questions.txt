1. Revenue Leakage — Where are we losing revenue?
What percentage of gross revenue is lost due to returns and cancellations?
Which products and countries contribute the most to revenue leakage?

2. Customer Value — Who generates the most revenue?
How much revenue is generated by Guest customers compared with Registered customers?
Who are our highest-value customers based on total net revenue generated?

3. Product & Market Performance — What is selling well?
Which products generate the highest gross revenue and net revenue?
Which products have high gross revenue but significantly lower net revenue due to returns?

4. Customer Churn Risk — Which customers are at risk?
Who are our highest-value customers based on their RFM characteristics?
Which previously valuable customers have not purchased recently and may be at risk of churn?
🎯 Main Business Question

Where are we making money, where are we losing money, and which customers should we worry about?

-----------------------------------------------------------------------------------------------------

# Online Retail II — End-to-End Project Requirements

**Goal:** Build a production-style BI/ML project on the Online Retail II dataset that goes beyond tutorial-level RFM/churn analysis, with enough methodological rigor and engineering depth to be a genuine CV/interview talking point for a master's student in Data/ML.

---

## Phase 0 — Data Foundation (mostly done)

- [x] Load `2009-2010` and `2010-2011` sheets, combine into one DataFrame
- [x] Handle missing `Customer ID` → treat as guest customers, encode as `0`
- [x] Check/handle duplicates and negative quantities (flag, don't drop — negatives are returns, not errors)
- [x] Compute `revenue = quantity * price`
- [x] Clean column names (lowercase, no spaces)
- [x] Load into SQLite
- [ ] **New:** Design a normalized schema instead of one flat table:
  - `transactions` (invoice_no, stock_code, customer_id FK, invoice_date, quantity, price, revenue)
  - `customers` (customer_id, country, first_seen, last_seen)
  - `products` (stock_code, description, avg_price)
- [ ] Add indexes on `customer_id`, `invoice_date`, `stock_code`
- [ ] Write a short `data_dictionary.md` documenting every column, type, and any business assumption (e.g., what `quantity < 0` means, how guests are encoded)

**Acceptance criteria:** SQLite DB with 3 normalized tables, indexed, queryable; assumptions documented, not just implied in code.

---

## Phase 1 — SQL Business Questions

For each question: write the SQL, save output as a table/view, write 2-3 sentences of interpretation. This becomes your findings section later.

### Q1. Revenue Leakage (in progress)
- Total refunded value and leakage as % of gross revenue
- Leakage by month, country, top returned products
- Distinguish "true returns" (matching a prior positive invoice) from standalone negative-quantity rows if possible

### Q2. RFM Segmentation
- Compute Recency, Frequency, Monetary per customer
- Bucket into segments (quantile-based, not arbitrary cutoffs — justify the binning method)
- Exclude guest customers (`customer_id = 0`) from per-customer analysis, note why

### Q3. Churn Definition (statistically justified, not arbitrary)
- Plot the distribution of inter-purchase intervals across the customer base
- Derive a churn cutoff from that distribution (e.g., a percentile of the gap distribution) instead of picking "90 days" by convention
- Document the reasoning as a modeling decision, with a sensitivity check (what changes if the cutoff moves ±30 days?)

### Q4. Cohort Retention
- Monthly acquisition cohorts based on first purchase date
- Retention curve per cohort (% still active in month N)

### Q5. Customer Lifetime Value (historical, simple)
- Historical CLV = total revenue per customer over observed period
- Optional stretch: segment CLV by RFM tier or cohort

**Acceptance criteria:** 5 SQL-answered questions, each with a saved output table and a short written interpretation (not just the query result).

---

## Phase 2 — Feature Engineering & Churn Model

- [ ] Build a feature table from RFM + cohort + product-diversity features (e.g., number of distinct categories bought, avg order value, days since first purchase)
- [ ] **Time-aware split, not random split**: train on behavior up to month T, predict churn in T+1 (prevents label leakage from future purchases)
- [ ] Baseline model: Logistic Regression
- [ ] Main model: XGBoost
- [ ] **Use Bayesian Optimization (not grid/random search) to tune XGBoost hyperparameters** — direct, honest tie-in to your thesis work; write a paragraph explaining why BO is more sample-efficient here
- [ ] Evaluate with precision/recall/F1/PR-AUC — explain why raw accuracy is misleading given class imbalance
- [ ] Add a short calibration check (reliability curve or Brier score) on the churn probabilities — ties back to your Bayesian background
- [ ] Feature importance / SHAP for interpretability

**Acceptance criteria:** A documented, time-aware churn model with a justified tuning method and honest evaluation (not just accuracy), plus an interpretability step.

---

## Phase 3 — Serving Layer (FastAPI)

- [ ] `GET /customers/{id}/rfm` — RFM values + segment
- [ ] `GET /customers/{id}/churn-risk` — churn probability from the trained model
- [ ] `GET /revenue-leakage/summary` — leakage KPIs (from Q1)
- [ ] `GET /cohorts/retention` — retention curve data
- [ ] Input validation (pydantic models), basic error handling (e.g., unknown customer_id)
- [ ] At least a handful of unit tests (pytest) for the endpoints and the feature-engineering functions

**Acceptance criteria:** API runs locally, documented endpoints (FastAPI auto-docs), basic test coverage — this is the layer most tutorial projects skip entirely.

---

## Phase 4 — Dashboard (Streamlit)

- [ ] Dashboard consumes the FastAPI endpoints (not SQLite directly) — realistic client/server architecture
- [ ] Pages: Revenue Leakage overview, RFM/segment explorer, Churn risk lookup, Cohort retention chart
- [ ] Keep it clean and interpretable — this is what a reviewer opens first

**Acceptance criteria:** Working dashboard, clearly calling the API layer, not duplicating query logic.

---

## Phase 5 — Packaging & Documentation

- [ ] `requirements.txt` / environment file
- [ ] Basic Dockerfile (API + dashboard, or just API to start)
- [ ] README with: problem framing, architecture diagram, key findings (with numbers — e.g., "identified €X leakage, Y% churn risk in top segment"), how to run it, and the methodological choices you made (time-aware split, BO tuning, churn cutoff derivation)
- [ ] Incremental, meaningful Git commits (not one final dump)

**Acceptance criteria:** A stranger could clone the repo, read the README, and understand both what the project does and why you made the choices you did — that "why" is what makes it read as more than a tutorial.

---

## What makes this defensible in an interview

Be ready to explain, in your own words:
1. Why guests (`customer_id = 0`) are handled separately, not dropped
2. Why the churn split is time-aware, not random
3. How you derived the churn cutoff from data instead of picking a round number
4. Why you used Bayesian Optimization for tuning and what it bought you over grid search
5. Why accuracy alone doesn't tell you if the churn model is good
6. What the API/dashboard split buys you architecturally over a single Streamlit script

If you can answer all six confidently, the project has done its job — regardless of how common the underlying dataset is.